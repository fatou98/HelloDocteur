{% extends 'layoutadmin.html.twig' %}


    {% block content %}
    <div>
  
  <table class="table table-striped table-hover" id="datatable3" cellspacing="0" width="100%">
                    <thead>
<tr>

<th>Adresse</th>
<th>Telephone</th>
<th>Patient</th>
<th>Motif</th>
<th>Actions</th>

</tr>
</thead><tbody>

{% for hads in Had %}
<tr>
 <td>{{ hads.adresse }}</td>
<td>{{ hads.Tel }}</td>
<td>{{ hads.patient.nomcomplet }}</td>
<td>{{ hads.motif }}</td>
<td><a href="{{ path('editHad', {'id' : hads.id }) }}" class="btn btn-rounded btn-primary btn-small">Editer</a><a href="#" class="btn btn-rounded btn-danger btn-small">Suprimer</a></td>
</tr>
{% endfor %}
</tbody> 



</table>
  </div>
{% endblock %}