{% extends 'layoutadmin.html.twig' %}


    {% block content %}
    <div>
   {% block flash %}
            {% for flash_message in app.session.flashbag.get('success') %}
                <div class="container alert alert-success" role="alert">
                    {{ flash_message }}
                </div>
            {% endfor %}
            {% for flash_message in app.session.flashbag.get('info') %}
                <div class="container alert alert-info" role="alert">
                    {{ flash_message }}
                </div>
            {% endfor %}
            {% for flash_message in app.session.flashbag.get('warning') %}
                <div class="container alert alert-warning" role="alert">
                    {{ flash_message }}
                </div>
            {% endfor %}
            {% for flash_message in app.session.flashbag.get('danger') %}
                <div class="container alert alert-danger" role="alert">
                    {{ flash_message }}
                </div>
                
            {% endfor %}
        {% endblock %}

  <table class="table table-striped table-hover" id="datatable3" cellspacing="0" width="100%">
                    <thead>
<tr>

<th>NomComplet</th>
<th>Telephone</th>
<th>Maticule</th>
<th>Specialite</th>
<th>Image</th>
<th>Editer</th>
</tr>
</thead><tbody>

{% for medecins in Medecin %}
<tr>
 <td>{{ medecins.nomcomplet }}</td>
<td>{{ medecins.tel }}</td>
<td>{{ medecins.matricule }}</td>
<td>{{ medecins.specialite.libelle }}</td>
<td><img src="data:image/png;base64,{{medecins.image}}" style="height:70px;border-radius:50%;" class="col-lg-2" ></td>
+
<td>
</td>
</tr>
{% endfor %}
</tbody> 



</table>
  </div>
{% endblock %}