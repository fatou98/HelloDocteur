

{% extends 'base.html.twig' %}

{% block body %}
  
    <style>
.container1
{
    background-image:url('img/img/r.jpg');
    background-size:cover;
    background-repeat:no-repeat;
    background-position-y:-750px;
    width:50%;
    margin:auto;
    padding-top:190px;
    padding-bottom:190px;
}
hr{
    margin-left:20%;
    margin-right:20%;
    color:white;
}
h2{
    text-align:center;
}
input{
    border:none;
    outline:none;
    background:white;
    border-bottom: solid 1px white;
    color:black;
}
button{
    border:none;
   height:38px;
    font-size:20px;
    margin:10px;
    border-radius:5px;
    
    color:white;
}


        </style>
        <div class="container container1 row">

        <h2>Connexion</h2>
        <hr>
          {% if error %}
        <div class="alert alert-danger">
            <ul class="list-unstyled mb-0">
                <li><span class="initialism form-error-icon badge badge-danger">Error</span> 
                    <span class="form-error-message">{{ error.messageKey|trans(error.messageData, 'security') }}</span>
                </li>
            </ul>
        </div>
    {% endif %}
            {{form(form)}}
            <br>
            <button class="btn">S'inscrire comme <a href="/register/2">ClIENT </a>|  <a href="/register/1">Berger </a> |  <a href="/register/3">ADMIN </a>
                   <span class="badge badge-success"></span>
            </button>
        </div>
{% endblock %}